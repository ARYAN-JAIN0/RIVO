# Core dependencies (required)
pandas
requests
streamlit

# Optional: Memory layer (graceful degradation if missing)
# Uncomment to enable vector/graph stores:
# chromadb
# networkx

# Optional: Invoice PDF generation
# reportlab